<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.mosque">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.InformasiActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.DonasiActivity" />
        <activity android:name=".view.activity.MasjidSekitarActivity" />
        <activity android:name=".view.activity.AcaraActivity" />
        <activity android:name=".view.activity.Informasi1Activity" />
        <activity android:name=".view.activity.DetailDonasiActivity" />
        <activity android:name=".view.activity.KeuanganActivity" />
    </application>

</manifest>